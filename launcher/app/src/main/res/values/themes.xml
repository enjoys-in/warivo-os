<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Platform Material theme: Compose supplies the real styling, and skipping
         AppCompat keeps the dependency list (and the APK) small. -->
    <style name="Theme.Warivo" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/warivo_black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
